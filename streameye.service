[Unit]
Description=tankontrol streameye webcam stream
After=network.target

[Service]
ExecStart=ffmpeg -v quiet -input_format mjpeg -video_size 1280x720 -i /dev/video0 -c copy -f mjpeg - | streameye

[Install]
WantedBy=multi-user.target
